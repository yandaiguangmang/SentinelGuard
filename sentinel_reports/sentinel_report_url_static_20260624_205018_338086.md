# SentinelGuard 哨塔检测报告

> 静态检测报告 · 风险等级：**HIGH** · 风险分数：**67/100**
> 证据分数：**0/100** · 深度研判分数：**- /100**
> 评分口径：URL 采用 `0.5 × 证据分数 + 0.5 × 深度研判分数`；APK 采用 `0.4 × 证据分数 + 0.3 × 深度研判分数 + 仲裁修正 + 鲁棒性奖励`。


## 一、检测结论
- 原始输入：`http://127.0.0.1:5000/`
- 对象类型：`url`
- 状态：`ready`
- 证据条数：2 条
- 高危证据：1 条

## 二、统一 IR 摘要
- 规范化 URL：`http://127.0.0.1:5000/`
- 协议：`http`
- 主机：`127.0.0.1`
- 路径：`/`
- 查询参数数量：`0`

## 三、跳转链
- http://127.0.0.1:5000/

## 四、页面线索
- 未获取页面内容线索。

## 四点一、页面线索

## 五、截图证据
- 当前未采集到页面截图。

## 六、风险证据
### 1. 未使用 HTTPS 加密
- 规则：`URL_NON_HTTPS`
- 严重级别：`low`
- 说明：目标网址使用明文 HTTP，访问过程可能被窃听或篡改。
- 证据：`http://127.0.0.1:5000/`
- 建议：优先访问 HTTPS 版本，不要在该页面输入账号、密码或验证码。

### 2. 使用 IP 地址直连
- 规则：`URL_IP_HOST`
- 严重级别：`high`
- 说明：目标使用 IP 地址而非可识别域名，常见于临时钓鱼页或规避域名信誉检测的页面。
- 证据：`127.0.0.1`
- 建议：核验该 IP 的真实归属，不建议直接信任 IP 地址登录页。


## 七、论坛式协同研判
### 主持人（模型：`unknown`）
综合 2 条证据，当前风险等级为 high。

### 静态分析员（模型：`unknown`）
URL 结构层面关注：使用 IP 地址直连。

### 行为分析员（模型：`unknown`）
页面行为与跳转层面关注暂未发现明显异常。

### 情报分析员（模型：`unknown`）
当前版本采用离线规则研判；若用于实战，建议补充域名信誉、证书透明度和黑名单情报。

### 处置建议员（模型：`unknown`）
建议阻断访问，不输入账号密码，不下载或运行页面提供的文件，并保留链接与报告用于复核。


## 八、扩展信息
- **web**：当前版本聚焦网页恶意检测：后续可继续扩展证书信誉、域名黑名单、跳转链溯源与页面界面线索比对。
- **apk**：当前版本支持 APK 静态检测：后续可继续扩展反编译、行为沙箱、证书信誉与动态通信分析。
