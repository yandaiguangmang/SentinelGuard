好的，我完全理解您的问题。您观察到的现象是：模型API调用均成功（HTTP 200），但部分角色的分析结果未能出现在最终报告中。

其根本原因在于，`APKDeepAnalyzer` 在处理模型返回结果时，如果遇到JSON解析失败或缺少关键字段（如`opinion`），会抛出异常并进入降级逻辑。但当前的降级处理只返回了一个简单的错误信息，而丢弃了模型实际返回的成功内容（即`result["content"]`）。

**核心解决思路是：** 当模型API调用成功但内容解析失败时，我们应将模型返回的原始内容作为“意见”保留下来，而不是直接丢弃。

以下是针对性的修改指令，请按照顺序依次操作。

---

### 修改指令

#### 1. 修改 `SentinelGuard/analyzers/apk_deep_analyzer.py` 文件

这个文件是APK深度分析的核心。我们需要修改`APKDeepAnalyzer`类中的两个关键方法。

##### 1.1 增强 `_normalize_role_output` 方法

**目标：** 当JSON解析失败时，捕获异常并将原始模型输出保存到返回结果中，而不是直接抛出异常。

**查找并替换：** 找到 `_normalize_role_output` 方法，将其整个函数体替换为下面的代码。

```python
    def _normalize_role_output(self, role: str, result: Dict[str, Any]) -> Dict[str, Any]:
        if not result.get("success"):
            raise ValueError(f"{role} 模型调用失败: {result.get('error') or '未知错误'}")

        # 保存原始内容，用于降级场景
        raw_content = result.get("content", "")

        try:
            data = json.loads(raw_content)
        except json.JSONDecodeError as exc:
            # 解析失败，构建一个包含原始内容的降级结果
            LOGGER.warning(f"{role} 模型返回的 JSON 无法解析，将使用原始内容降级。错误: {exc}")
            fallback_findings = _coerce_additional_findings(
                [],
                default_role=role,
                default_rule_prefix=f"DEEP_{role.upper()}_PARSE_ERROR",
                default_description=f"{role} 模型返回了非标准JSON，已降级使用原始文本。",
                default_evidence=raw_content[:500], # 截断原始内容作为证据
                default_recommendation="检查模型输出格式是否符合预期。",
            )
            # 返回一个结构，opinion 直接使用原始内容
            return {
                "opinion": f"模型返回原始内容（非标准JSON）: {raw_content[:1000]}", # 保留前1000字符
                "risk_hint": "medium", # 降级默认中危
                "additional_findings": fallback_findings,
                "claim": "uncertain",
                "confidence": 0.0,
            }

        # 原有的正常解析逻辑
        opinion = str(data.get("opinion") or data.get("summary") or "").strip()
        if not opinion:
            # 如果解析出的opinion为空，使用原始内容作为降级
            LOGGER.warning(f"{role} 模型输出缺少 opinion 字段，将使用原始内容降级。")
            fallback_findings = _coerce_additional_findings(
                [],
                default_role=role,
                default_rule_prefix=f"DEEP_{role.upper()}_MISSING_OPINION",
                default_description=f"{role} 模型输出缺少'opinion'字段，已降级使用原始内容。",
                default_evidence=raw_content[:500],
                default_recommendation="检查模型输出格式，确保包含'opinion'字段。",
            )
            return {
                "opinion": f"模型返回内容（缺少opinion字段）: {raw_content[:1000]}",
                "risk_hint": "medium",
                "additional_findings": fallback_findings,
                "claim": "uncertain",
                "confidence": 0.0,
            }

        # 正常情况
        findings = _coerce_additional_findings(
            data.get("additional_findings"),
            default_role=role,
            default_rule_prefix=f"DEEP_{role.upper()}_SIGNAL",
            default_description=f"{role} 发现额外风险线索。",
            default_evidence="",
            default_recommendation="结合静态报告进一步复核。",
        )

        normalized: Dict[str, Any] = {
            "opinion": opinion,
            "risk_hint": _normalize_risk_level(data.get("risk_hint"), "medium", 50),
            "additional_findings": findings,
        }

        claim = str(data.get("claim") or "").strip().lower()
        normalized["claim"] = claim if claim in {"malicious_lean", "benign_lean", "uncertain"} else "uncertain"

        confidence = data.get("confidence")
        normalized["confidence"] = max(0.0, min(1.0, float(confidence))) if isinstance(confidence, (int, float)) else 0.0

        return normalized
```

##### 1.2 修改 `_normalize_result` 方法以保留降级输出

**目标：** 确保在主持人总结失败时，我们仍然能保留前面四个角色的输出。

**查找并替换：** 找到 `_normalize_result` 方法，它目前会抛出异常。我们将其改为一个更宽容的版本，当主持人输出解析失败时，构造一个合理的降级结果。

```python
    def _normalize_result(
        self,
        result: Dict[str, Any],
        static_report: DetectionReport,
        role_outputs: Dict[str, Dict[str, Any]],
        arbitration_result: Any = None,
        robustness_result: Any = None,
        role_scores: Dict[str, int] | None = None,
    ) -> Dict[str, Any]:
        if not result.get("success"):
            raise ValueError(result.get("error") or "模型深度检查失败")

        raw_content = result.get("content", "")
        try:
            data = json.loads(raw_content)
        except json.JSONDecodeError as exc:
            # 主持人JSON解析失败，构建降级结果
            LOGGER.warning(f"主持人模型返回的JSON无法解析，将使用原始内容降级。错误: {exc}")
            return self._build_host_fallback_result(
                static_report,
                role_outputs,
                arbitration_result,
                robustness_result,
                role_scores,
                error=f"主持人JSON解析失败: {exc}",
                raw_content=raw_content, # 传入原始内容
            )

        # 原有的正常解析逻辑
        expert_opinions = _coerce_mapping(data.get("expert_opinions"))
        normalized_opinions = {role: str(expert_opinions.get(role) or "") for role in DEEP_ROLE_ORDER}
        # ... (此处保留原有逻辑，直到最后的return)
        # 需要确保在return之前，如果主持人opinion为空，使用降级
        if not normalized_opinions.get("主持人", "").strip():
            summary = str(data.get("summary") or "")
            if summary:
                normalized_opinions["主持人"] = summary
            else:
                # 如果summary也为空，使用角色输出的摘要构建
                summary = self._build_host_fallback_summary(static_report, role_outputs, score, risk_level, "主持人输出缺少总结")
                normalized_opinions["主持人"] = summary

        # ... (其余代码不变，最终返回)
```

##### 1.3 增强 `_build_host_fallback_result` 方法以支持原始内容

**目标：** 允许 `_build_host_fallback_result` 方法接受一个原始的模型内容，并将其包含在最终的总结中，以便用户能看到模型实际返回了什么。

**查找并替换：** 找到 `_build_host_fallback_result` 方法，将其签名和主体替换为下面的代码。

```python
    def _build_host_fallback_result(
        self,
        static_report: DetectionReport,
        role_outputs: Dict[str, Dict[str, Any]],
        arbitration_result: Any = None,
        robustness_result: Any = None,
        role_scores: Dict[str, int] | None = None,
        error: Any = None,
        raw_content: str = "", # 新增参数
    ) -> Dict[str, Any]:
        fallback_findings: List[DetectionFinding] = []
        for role_output in role_outputs.values():
            fallback_findings.extend(_coerce_findings(role_output.get("additional_findings")))

        combined_scores = role_scores or self._extract_role_scores(role_outputs, static_report)
        evidence_score = score_from_findings(static_report.findings + fallback_findings)
        host_score = combine_apk_scores(
            evidence_score,
            combined_scores.get("静态分析员", static_report.score),
            arbitration_result,
            robustness_result,
        )
        risk_level = risk_level_from_score(host_score)
        summary = self._build_host_fallback_summary(static_report, role_outputs, host_score, risk_level, error)
        
        # 如果有原始内容，将其追加到总结中
        if raw_content:
            summary = f"{summary}\n\n主持人原始返回内容（解析失败）:\n{raw_content[:2000]}" # 保留前2000字符

        expert_opinions = {
            role: str(role_outputs.get(role, {}).get("opinion") or static_report.expert_opinions.get(role) or "")
            for role in DEEP_ROLE_ORDER
        }
        expert_opinions["主持人"] = summary

        return {
            "risk_level": risk_level,
            "score": host_score,
            "deep_score": host_score,
            "evidence_score": evidence_score,
            "expert_opinions": expert_opinions,
            "expert_models": self.role_models,
            "deep_summary": summary,
            "additional_findings": fallback_findings,
            "role_scores": combined_scores,
            "arbitration_result": arbitration_result.to_dict() if hasattr(arbitration_result, "to_dict") else _coerce_mapping(arbitration_result),
            "robustness_result": robustness_result.to_dict() if hasattr(robustness_result, "to_dict") else _coerce_mapping(robustness_result),
        }
```

#### 2. 修改 `SentinelGuard/analyzers/url_deep_analyzer.py` 文件

URL深度分析器也存在类似的问题，需要同步修改以保持一致性。

##### 2.1 增强 `_normalize_role_output` 方法 (URL版本)

**查找并替换：** 找到 `URLDeepAnalyzer` 类中的 `_normalize_role_output` 方法，将其替换为与APK版本逻辑相同的代码。

```python
    def _normalize_role_output(self, role: str, result: Dict[str, Any]) -> Dict[str, Any]:
        if not result.get("success"):
            raise ValueError(f"{role} 模型调用失败: {result.get('error') or '未知错误'}")

        raw_content = result.get("content", "")

        try:
            data = _load_json_payload(raw_content)
        except ValueError as exc:
            LOGGER.warning(f"{role} 模型返回的 JSON 无法解析，将使用原始内容降级。错误: {exc}")
            fallback_findings = _coerce_additional_findings(
                [],
                default_role=role,
                default_rule_prefix=f"DEEP_{role.upper()}_PARSE_ERROR",
                default_description=f"{role} 模型返回了非标准JSON，已降级使用原始文本。",
                default_evidence=raw_content[:500],
                default_recommendation="检查模型输出格式是否符合预期。",
            )
            return {
                "opinion": f"模型返回原始内容（非标准JSON）: {raw_content[:1000]}",
                "risk_hint": "medium",
                "additional_findings": fallback_findings,
                "claim": "uncertain",
                "confidence": 0.0,
            }

        opinion = str(data.get("opinion") or data.get("summary") or "").strip()
        if not opinion:
            LOGGER.warning(f"{role} 模型输出缺少 opinion 字段，将使用原始内容降级。")
            fallback_findings = _coerce_additional_findings(
                [],
                default_role=role,
                default_rule_prefix=f"DEEP_{role.upper()}_MISSING_OPINION",
                default_description=f"{role} 模型输出缺少'opinion'字段，已降级使用原始内容。",
                default_evidence=raw_content[:500],
                default_recommendation="检查模型输出格式，确保包含'opinion'字段。",
            )
            return {
                "opinion": f"模型返回内容（缺少opinion字段）: {raw_content[:1000]}",
                "risk_hint": "medium",
                "additional_findings": fallback_findings,
                "claim": "uncertain",
                "confidence": 0.0,
            }

        findings = _coerce_additional_findings(
            data.get("additional_findings"),
            default_role=role,
            default_rule_prefix=f"DEEP_{role.upper()}_SIGNAL",
            default_description=f"{role} 发现额外风险线索。",
            default_evidence="",
            default_recommendation="结合静态报告进一步复核。",
        )

        normalized: Dict[str, Any] = {
            "opinion": opinion,
            "risk_hint": _normalize_risk_level(data.get("risk_hint"), "medium", 50),
            "additional_findings": findings,
        }

        claim = str(data.get("claim") or "").strip().lower()
        normalized["claim"] = claim if claim in {"malicious_lean", "benign_lean", "uncertain"} else "uncertain"

        confidence = data.get("confidence")
        normalized["confidence"] = max(0.0, min(1.0, float(confidence))) if isinstance(confidence, (int, float)) else 0.0

        recommended_action = str(data.get("recommended_action") or "").strip().lower()
        if recommended_action in {"block", "sandbox_review", "monitor", "allow"}:
            normalized["recommended_action"] = recommended_action

        return normalized
```

##### 2.2 修改 `_normalize_result` 方法 (URL版本)

**查找并替换：** 找到 `URLDeepAnalyzer` 类中的 `_normalize_result` 方法，让它在主持人解析失败时也能构建包含原始内容的降级结果。由于URL版没有现成的 `_build_host_fallback_result`，我们将在 `_normalize_result` 内部直接处理。

```python
    def _normalize_result(self, result: Dict[str, Any], static_report: DetectionReport, role_outputs: Dict[str, Dict[str, Any]]) -> Dict[str, Any]:
        if not result.get("success"):
            raise ValueError(result.get("error") or "模型深度检查失败")

        raw_content = result.get("content", "")
        try:
            data = _load_json_payload(raw_content)
        except ValueError as exc:
            LOGGER.warning(f"主持人模型返回的JSON无法解析，将使用原始内容降级。错误: {exc}")
            # 构建一个降级的结果
            return self._build_degraded_result(
                static_report,
                role_outputs,
                error=f"主持人JSON解析失败: {exc}",
                raw_content=raw_content, # 传入原始内容
            )

        # --- 以下为原有逻辑，但是需要修改 _build_degraded_result 或直接在这里处理 ---
        # 为了最小改动，我们修改 _build_degraded_result 使其支持 raw_content
        # ... (原有代码不变，只是最后如果出错，会调用下面的增强版 _build_degraded_result)
```

##### 2.3 增强 `_build_degraded_result` 方法 (URL版本)

**查找并替换：** 找到 `URLDeepAnalyzer` 类中的 `_build_degraded_result` 方法，修改其签名和逻辑以支持 `raw_content`。

```python
    def _build_degraded_result(self, static_report: DetectionReport, role_outputs: Dict[str, Dict[str, Any]], error: Any, raw_content: str = "") -> Dict[str, Any]:
        # 收集所有已产出的发现
        fallback_findings: List[DetectionFinding] = list(static_report.findings)  # 静态发现
        for role_output in role_outputs.values():
            fallback_findings.extend(_coerce_findings(role_output.get("additional_findings")))

        evidence_score = _score_from_findings(fallback_findings)
        score = evidence_score
        risk_level = _risk_level_from_score(score)

        fallback_opinions = {
            role: str(role_outputs.get(role, {}).get("opinion") or static_report.expert_opinions.get(role) or "")
            for role in DEEP_ROLE_ORDER
        }
        summary = f"深度研判主持人阶段失败，已根据静态检测与四名专家的发现重新计算风险。原因：{error}"
        if raw_content:
            summary = f"{summary}\n\n主持人原始返回内容（解析失败）:\n{raw_content[:2000]}"
        fallback_opinions["主持人"] = summary

        return {
            "risk_level": risk_level,
            "score": score,
            "expert_opinions": fallback_opinions,
            "expert_models": self.role_models,
            "deep_summary": summary,
            "additional_findings": fallback_findings,
            "consensus_check": {"agreement_level": "unknown", "conflicting_signals": []},
        }
```

### 总结

完成以上修改后，您的程序将具备更强的鲁棒性：

1.  **角色输出健壮性**：即使模型返回了非标准JSON或缺少关键字段，`_normalize_role_output` 也不会抛出异常，而是会构建一个包含原始内容的降级结果。这个结果会被 `role_outputs` 捕获并传递给主持人。
2.  **主持人输出健壮性**：如果主持人的输出无法解析，`_build_host_fallback_result` 或 `_build_degraded_result` 会保留原始内容。这些内容会出现在最终的 `expert_opinions["主持人"]` 中，并被渲染到Markdown和HTML报告里。

这样，您就可以在报告中看到所有角色（包括解析失败的角色）的原始输出，方便调试和分析。